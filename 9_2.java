public class AnagramComparator implements Comparator<String> {
    public String sortChars(String s){
        char[] content = s.toCharArray;
        Array.sort(content);
        return new Stringn(content);
    }
    
    public int compare(String s1, String s2){
        return sortChars(s1).compareTo(sortChar(s2));
    }
}


Array.sort(array, new AnagramComparator());



public class AnagramComparator implements Comparato<String>{
    public String sortChar(String s){
        char[] content = s.toCharArray();
        Array.sort(contant);
        return new string(content);
    }
    public int compare(string s1, string s2){
        return sortChar(s1).compareTo(sortChar(s2));
    }
}



public class AnagramComparato implements Comparator<String>{
    public String sortChars(String s){
        char[] contents = s.toCharArray;
        Array.sort(contents);
        return new contents;
    }
    public int compare(string s1,String s2){
        return sortChar(s1).compareTo(sortChar(s2));
    }
}

public class AnagramComparato implements Comparartor<String>{
    public String sorChar(String s){
        char[] contents = s.toCharArray;
        Array.sort(content);
        return new String(contents);
    }
    public int compare(String s1, String s2){
        return sortChar(s1).compareTo(sortchar(s2));
    }
}

